package day67_generics;

public class GenericsEntry {

	public static void main(String[] args) {
		
	
	/*
	** In a nutshell, generics enable types (classes and interfaces) 
	to be parameters when defining classes, interfaces and methods. 
	** Much like the more familiar formal parameters used in method declarations, 
	type parameters provide a way for us 
	to re-use the same code with different inputs. 
	*** Code that uses generics has many benefits over non-generic code:
	** Stronger type checks at compile time.
		A Java compiler applies strong type checking to generic code and 
		issues errors if the code violates type safety. 
		Fixing compile-time errors is easier than fixing runtime errors,
		which can be difficult to find.
	** Elimination of casts.
		For instance, the following code snippet without generics requires casting:
	*/
	
	
		
		
//		List list1 = new ArrayList();
//		list1.add("hello");
//		String s = (String) list1.get(0);
//		
//		
//		// When re-written to use generics, the code does not require casting:
//		List<String> list2 = new ArrayList<>();
//		list2.add("hello");
//		String s2 = list2.get(0);   // no cast
	
		
		
		
	}
}
